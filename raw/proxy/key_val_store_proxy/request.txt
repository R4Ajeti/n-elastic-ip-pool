Target URL:
- https://api.keyval.org/get/{KEY}
- https://api.keyval.org/set/{KEY}/{VALUE}
HTTP method: GET
Required headers:
- Accept: text/plain
Authentication: None. KeyVal REST URL values are public, so never send secrets or real proxy credentials.

This request documents the KeyValStoreProxy calls used by the service layer. The service sends only a hashed safe dummy proxy value. The proxy layer handles the KeyVal endpoint URL, GET request construction, timeout, response parsing, and provider errors.
